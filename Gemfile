source "https://rubygems.org"

# Matches the gem GitHub Pages uses, so local preview == deployed site.
gem "github-pages", group: :jekyll_plugins
gem "webrick", "~> 1.8"  # needed for local serve on Ruby 3+
